namespace: default
resources:
  # - mariadb.yaml
  - backend.yaml
  - deployment.yaml
  - nginx.yaml
  - redis.yaml
configMapGenerator:
  - name: nginx
    files:
      - configs/default.conf
  - name: init-db
    files:
      - configs/init.sql
